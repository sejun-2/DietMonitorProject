<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC 
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="user_mapper">

	<select id="findLoginUser" parameterType="User" resultType="User">
		SELECT * FROM member WHERE email=#{email} and pw=#{pw}
	</select>

	<select id="findUserList" resultType="User">
		SELECT * FROM member
	</select>
	
	<select id="findUserByEmail" resultType="User" parameterType="String">
		SELECT * FROM member
		WHERE email = #{email}
	</select>
	
	<update id="modifyUser" parameterType="User">
		UPDATE member
		SET pw = #{pw},
			name = #{name},
		WHERE email = #{email}
	</update>
	
	<insert id="saveUser" parameterType="User">
		INSERT INTO member
		VALUES ( member_id_seq.nextval, #{email}, #{pw}, #{name},  #{tel}, #{birth}, #{gender_id},  sysdate )
	</insert>
	
	<select id="getAgeByEmail" parameterType="String" resultType="int">
        SELECT CEIL(MONTHS_BETWEEN(SYSDATE, birth) / 12) AS age
        FROM member
        WHERE email = #{email}
    </select>
    
    <select id="getGenderNameByGenderId" parameterType="int" resultType="String">
        SELECT gender_name
		FROM gender
		WHERE gender_id = #{genderId}
    </select>
    
     <select id="getEmailCountByEmail" parameterType="String" resultType="int">
        SELECT COUNT(*)
		  FROM member
		 WHERE email = #{email}
    </select>
	
</mapper>					


	