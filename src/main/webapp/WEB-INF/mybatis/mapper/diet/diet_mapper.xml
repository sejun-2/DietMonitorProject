<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC 
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="diet_mapper">

	<select id="findTotalDiet" parameterType="User"
		resultType="Diet">
		SELECT * FROM total_diet
		WHERE account_no = #{accountNo} AND
		member_no = #{memberNo}
	</select>

	<select id="findDailyDiet" parameterType="User"
		resultType="Diet">
		SELECT * FROM daily_diet
		WHERE account_no = #{accountNo} AND
		member_no = #{memberNo}
	</select>

	<insert id="addFoodToDailyDiet" parameterType="Diet">
		INSERT INTO
		daily_diet
		VALUES( log_no_seq.nextval, #{accountNo}, #{memberNo}, SYSDATE, #{foodCode},
		#{foodIntake}, #{kcal}, #{water}, #{protein}, #{fat}, #{batch},
		#{carbohydrate}
		, #{sugars}, #{dietaryFiber}, #{calcium}, #{ironContent}, #{phosphorus}
		, #{potassium}, #{sodium}, #{vitaminA}, #{retinol}, #{betaCarotene},
		#{thiamine}
		, #{riboflavin}, #{niacin}, #{vitaminC}, #{vitaminD}, #{cholesterol}
		, #{saturatedFat}, #{transFat}, 1 )
	</insert>


	<select id="findFoodListByMemberInfo" parameterType="User"
		resultType="Diet">
		SELECT daily_diet.*, food.food_name, food.main_category_name
		FROM (SELECT * FROM daily_diet
			WHERE account_no = #{accountNo} AND member_no = #{memberNo}) daily_diet,
		(SELECT
		food_code,
		food_name,
		data_sort_id,
		data_sort_name,
		food_sort_id,
		food_sort_name,
		main_category_id,
		main_category_name,
		rep_food_id,
		rep_food_name,
		mid_category_id,
		mid_category_name,
		sub_category_id,
		sub_category_name,
		min_category_id,
		min_category_name,
		nut_standard_amount,
		kcal,
		water,
		protein,
		fat,
		batch,
		carbohydrate,
		sugars,
		dietary_fiber,
		calcium,
		iron_content,
		phosphorus,
		potassium,
		sodium,
		vitamin_a,
		retinol,
		beta_carotene,
		thiamine,
		riboflavin,
		niacin,
		vitamin_c,
		vitamin_d,
		cholesterol,
		saturated_fat,
		trans_fat,
		source_code,
		source_name,
		ref_amount_once,
		food_weight,
		manufacturing_number,
		manufacturing_company,
		data_generation_method_code,
		data_generation_method_name,
		data_creation_date,
		data_base_date
		FROM food_list_processed
		UNION ALL
		SELECT
		food_code,
		food_name,
		data_sort_id,
		data_sort_name,
		food_sort_id,
		food_sort_name,
		main_category_id,
		main_category_name,
		rep_food_id,
		rep_food_name,
		mid_category_id,
		mid_category_name,
		sub_category_id,
		sub_category_name,
		min_category_id,
		min_category_name,
		nut_standard_amount,
		kcal,
		water,
		protein,
		fat,
		batch,
		carbohydrate,
		sugars,
		dietary_fiber,
		calcium,
		iron_content,
		phosphorus,
		potassium,
		sodium,
		vitamin_a,
		retinol,
		beta_carotene,
		thiamine,
		riboflavin,
		niacin,
		vitamin_c,
		vitamin_d,
		cholesterol,
		saturated_fat,
		trans_fat,
		source_code,
		source_name,
		null,
		food_weight,
		null,
		manufacturing_company,
		data_generation_method_code,
		data_generation_method_name,
		data_creation_date,
		data_base_date
		FROM food_list_cooked
		UNION ALL
		SELECT
		food_code,
		food_name,
		data_sort_id,
		data_sort_name,
		food_sort_id,
		food_sort_name,
		main_category_id,
		main_category_name,
		rep_food_id,
		rep_food_name,
		mid_category_id,
		mid_category_name,
		sub_category_id,
		sub_category_name,
		min_category_id,
		min_category_name,
		nut_standard_amount,
		kcal,
		water,
		protein,
		fat,
		batch,
		carbohydrate,
		sugars,
		dietary_fiber,
		calcium,
		iron_content,
		phosphorus,
		potassium,
		sodium,
		vitamin_a,
		retinol,
		beta_carotene,
		thiamine,
		riboflavin,
		niacin,
		vitamin_c,
		vitamin_d,
		cholesterol,
		saturated_fat,
		trans_fat,
		source_code,
		source_name,
		null,
		null,
		null,
		null,
		data_generation_method_code,
		data_generation_method_name,
		data_creation_date,
		data_base_date
		FROM food_list_agriculture
		UNION ALL
		SELECT
		food_code,
		food_name,
		data_sort_id,
		data_sort_name,
		food_sort_id,
		food_sort_name,
		main_category_id,
		main_category_name,
		rep_food_id,
		rep_food_name,
		mid_category_id,
		mid_category_name,
		sub_category_id,
		sub_category_name,
		min_category_id,
		min_category_name,
		nut_standard_amount,
		kcal,
		water,
		protein,
		fat,
		batch,
		carbohydrate,
		sugars,
		dietary_fiber,
		calcium,
		iron_content,
		phosphorus,
		potassium,
		sodium,
		vitamin_a,
		retinol,
		beta_carotene,
		thiamine,
		riboflavin,
		niacin,
		vitamin_c,
		vitamin_d,
		cholesterol,
		saturated_fat,
		trans_fat,
		source_code,
		source_name,
		null,
		null,
		null,
		null,
		data_generation_method_code,
		data_generation_method_name,
		data_creation_date,
		data_base_date
		FROM food_list_fisheries) food
		WHERE daily_diet.food_code = food.food_code
	</select>

	<select id="findTotalNutrientFromDailyDietByMemberInfo"
		parameterType="User" resultType="Diet">
		SELECT SUM(kcal) kcal, SUM(water)
		water, SUM(protein) protein, SUM(fat) fat
		, SUM(carbohydrate) carbohydrate, SUM(sugars) sugars
		, SUM(dietary_fiber) dietary_fiber, SUM(calcium) calcium
		, SUM(iron_content) iron_content, SUM(phosphorus) phosphorus
		, SUM(potassium) potassium, SUM(sodium) sodium, SUM(vitamin_a)
		vitamin_a
		, SUM(retinol) retinol, SUM(beta_carotene) beta_carotene
		, SUM(thiamine) thiamine, SUM(riboflavin) riboflavin
		, SUM(niacin) niacin, SUM(vitamin_c) vitamin_c, SUM(vitamin_d)
		vitamin_d
		, SUM(cholesterol) cholesterol, SUM(saturated_fat) saturated_fat
		, SUM(trans_fat) trans_fat
		FROM daily_diet
		WHERE account_no =
		#{accountNo} AND member_no = #{memberNo}
	</select>
	
	<delete id="deleteDiet" parameterType="Diet">
		DELETE FROM daily_diet
		WHERE log_no = #{logNo}
	</delete>


</mapper>